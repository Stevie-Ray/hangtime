import state from './progress.state'
import mutations from './progress.mutations'
import actions from './progress.actions'
import getters from './progress.getters'

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
